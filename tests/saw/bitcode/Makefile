include ../../../s2n.mk

BCS=$(wildcard *.bc)

%_llvm.bc: $(BCS)
	$(LLVMLINK) -o $@ $+

# $+ 和 $^ 都是 Makefile 中的自动化变量，用于表示依赖文件列表。
# $+ 表示当前规则中所有的依赖文件列表，其中包括重复的依赖文件。如果一个规则的依赖文件列表中有重复的文件，那么 $+ 将包含这些重复的文件。
# $^ 表示当前规则中所有的依赖文件列表，其中不包括重复的依赖文件。如果一个规则的依赖文件列表中有重复的文件，那么 $^ 将只包含这些文件的一个副本。

